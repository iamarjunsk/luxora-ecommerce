{
    "buildCommand": "cd luxora && npx prisma generate && npm run build",
    "outputDirectory": "luxora/.output",
    "installCommand": "cd luxora && npm install",
    "framework": "nuxtjs",
    "regions": [
        "bom1"
    ]
}